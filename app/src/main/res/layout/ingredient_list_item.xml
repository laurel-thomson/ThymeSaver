<?xml version="1.0" encoding="utf-8"?>
<android.support.constraint.ConstraintLayout
    android:layout_width="match_parent"
    android:layout_height="50dp"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:background="@color/colorTextIcon"
    android:orientation="horizontal"
    xmlns:android="http://schemas.android.com/apk/res/android">

    <TextView
        style="@style/TextViewStyle"
        android:id="@+id/ingredient_textview"
        android:layout_width="wrap_content"
        android:layout_height="match_parent"
        app:layout_constraintStart_toStartOf="parent"/>

    <LinearLayout
        android:id="@+id/decrement_quantity_layout"
        app:layout_constraintEnd_toStartOf="@id/ingredient_quantity_picker"
        android:gravity="center"
        android:layout_width="60dp"
        android:layout_height="match_parent">

        <ImageView
            android:id="@+id/decrement_ing_button"
            android:layout_width="wrap_content"
            android:layout_height="match_parent"
            android:layout_gravity="center_horizontal"
            android:src="@drawable/ic_remove"/>

    </LinearLayout>


    <TextView
        style="@style/TextViewStyle"
        android:id="@+id/ingredient_quantity_picker"
        android:layout_width="wrap_content"
        android:layout_gravity="center"
        app:layout_constraintEnd_toStartOf="@+id/increment_quantity_layout"
        android:layout_height="match_parent"/>

    <LinearLayout
        android:id="@+id/increment_quantity_layout"
        app:layout_constraintEnd_toEndOf="parent"
        android:gravity="center"
        android:layout_width="60dp"
        android:layout_height="match_parent">

        <ImageView
            android:id="@+id/increment_ing_button"
            android:layout_width="wrap_content"
            android:layout_height="match_parent"
            android:layout_gravity="center_horizontal"
            android:src="@drawable/ic_add"/>

    </LinearLayout>

</android.support.constraint.ConstraintLayout>